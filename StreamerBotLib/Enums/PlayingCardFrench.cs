namespace StreamerBotLib.Enums
{
    public enum PlayingCardFrench
    {
        // added 'K' to the start so the _2, _3... has the same position number as the card value.
        // interpret the 'K' to a 10-value, 'A' as 1 or 11, 'J' & 'Q' as 10 - in number card games
        K, A, _2, _3, _4, _5, _6, _7, _8, _9, _10, J, Q
    }
}
