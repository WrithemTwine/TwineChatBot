namespace StreamerBot.Enum
{
    public enum DefaultCommand { 
        commands, 
        addcommand, 
        lurk, 
        unlurk, 
        worklurk, 
        socials, 
        bot, 
        so, 
        join, enqueue, 
        leave, dequeue, 
        queue, 
        qinfo, 
        qstart, 
        qstop, 
        follow, 
        watchtime, 
        uptime, 
        followage }
}
