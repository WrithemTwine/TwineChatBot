namespace ChatBot_Net5.Enum
{
    public enum DefaultCommand { commands, addcommand, lurk, unlurk, worklurk, socials, bot, so, join, leave, queue, qinfo, qstart, qstop, follow, watchtime, uptime, followage }
}
